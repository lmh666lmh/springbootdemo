spring:
  datasource:
    url: jdbc:mysql:///db_person?characterEncoding=utf8&useUnicode=true&useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
server:
  port: 8081
  servlet:
        context-path: /springboot
